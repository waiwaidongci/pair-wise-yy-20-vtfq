# 变奏预演（临时加花）

排练时对某一小节临时加花，先用"变奏预演"在试听区出 **稀疏 / 适中 / 密集** 三份候选，
确认采纳后才替换该小节；其余谱面与已存方案不动。

## 用法

1. 在"变奏预演"里选择目标小节，点 **生成三份候选**。
2. 三份候选停在试听区，口令格（强拍四声部齐奏位）整格描金保留，绝不改动。
3. 点某份候选的 **试听**（或顶部"播放"）只循环响这一小节的候选，主谱不响。
4. 满意后点该份的 **采纳**：仅替换该小节；不满意点 **放弃候选**，原谱保留。
5. 刷新或关闭页面再打开，未采纳的候选仍在试听区，可继续试听。

失败时一律保留原谱，并在试听区下方写明：冲突小节、保留口令格（第几拍）、失败原因。

- **保留点太满**：该小节口令骨架之外的空白格少于 3 个，凑不齐三档。
- **三份凑不齐 / 与当前相同**：受限情况下无法生成三份互不相同且不同于现谱的候选。

## 三层拆分

| 文件 | 职责 | 依赖 |
| --- | --- | --- |
| `js/variations.js` | 生成规则：口令格判定、确定性随机、三档候选、失败判定（纯函数） | 无 |
| `js/audition.js` | 候选存档：试听会话持久化、采纳只改目标小节、冲突检测 | `variations.js` 常量 |
| `app.js` | 页面操作：渲染、锁定目标小节、试听播放、采纳/放弃交互 | 上面两者 + DOM |

## 可复现性

候选由 `mulberry32(seed)` 确定性生成；种子 = 当前谱面 + 目标小节 + 生成轮次。
同一谱面同一轮次永远得到同三份候选（种子显示在试听区页脚）。"重新生成"推进轮次，
得到新的一批且仍可复现。

## 测试

```
node test/variations.test.mjs   # 生成规则：三份互异/可复现/口令保留/失败分支
node test/audition.test.mjs     # 存档：采纳隔离/冲突检测/会话往返
node test/app.smoke.mjs         # 页面：生成→试听→采纳全流程（最小 DOM 桩）
```
